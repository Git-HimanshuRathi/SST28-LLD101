package com.example.elevator;

/**
 * Elevator state: UP, DOWN, IDLE (on a particular floor), MAINTENANCE.
 * Replaces/extends Direction to include MAINTENANCE state.
 */
public enum ElevatorState { UP, DOWN, IDLE, MAINTENANCE }
